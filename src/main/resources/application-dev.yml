spring:
  jpa:
    hibernate:
      ddl-auto: update
  docker:
    compose:
      enabled: false
      file: ./docker/docker-compose.yml
      lifecycle-management: start-only
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://cj-dev-mysql.c5syk8q4mlad.ap-northeast-2.rds.amazonaws.com:3306/cj
    username: ${SPRING_DATASOURCE_USERNAME}
    password: ${SPRING_DATASOURCE_PASSWORD}
  data:
    redis:
      host: ${SPRING_DATA_REDIS_HOST}
      port: 6379
springdoc:
  swagger-ui:
    path: /swagger-ui/index.html
    config-url: /app/v3/api-docs/swagger-config  # 🔥 핵심
  api-docs:
    path: /app/v3/api-docs  # 🔥 핵심